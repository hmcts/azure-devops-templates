steps:
  - task: GitVersion@4
    displayName: GitVersion
    inputs:
      preferBundledVersion: false

  - task: NuGetToolInstaller@0
    displayName: 'Use NuGet ${{ parameters.name }}'
    inputs:
      versionSpec: ${{ parameters.name }}
