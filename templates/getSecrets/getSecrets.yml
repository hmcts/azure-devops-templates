parameters:
  azureSubscription: 'HMCTS_SonarCloud'
  sonarCloudOrganization: 'hmcts'
  KeyVaultName: ''
  SecretsFilter: ''


steps:
  - task: AzureKeyVault@1
    displayName: 'Azure Key Vault: vhcoreinfrapreview'
    inputs:
      azureSubscription: '${{ parameters.azureSubscription }}'
      KeyVaultName: '${{ parameters.KeyVaultName }}'
      SecretsFilter: '${{ parameters.SecretsFilter }}'
