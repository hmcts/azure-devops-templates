parameters:
  buildConfiguration: 'Release'
  apiDirectory: ''

steps:
  - task: DotNetCoreCLI@2
    displayName: Publish
    inputs:
      command: publish
      publishWebProjects: false
      arguments: '--configuration ${{ parameters.buildConfiguration }} --output $(build.artifactstagingdirectory)\WebApp -r win-x64'
      zipAfterPublish: True
      workingDirectory: '${{ parameters.apiDirectory }}'
